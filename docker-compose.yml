version: '3.8'

services:
  webstar-server:
    build: ./webstar-server-dotnet
    ports:
      - "5090:5090"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:5090
    restart: unless-stopped
